spring:
  application:
    name: computing-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/order_service_so16
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update

  mvc:
    hiddenmethod:
      filter:
        enabled: true

server:
  port: 0

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8167/eureka
